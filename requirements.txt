# Single source of dependency truth for the whole project.
# HF Spaces installs from this file; pyproject.toml reads the same list via
# [tool.setuptools.dynamic], so `pip install -e .` (CI) and the Space stay in
# sync with no second list to maintain.
pydantic>=2
python-dotenv>=1
psycopg[binary,pool]>=3.1  # [pool] = psycopg_pool, the app's shared Neon pool
anthropic>=0.40
google-genai>=1
openai>=1.50
sentence-transformers>=3
langgraph>=0.2
requests>=2.31
beautifulsoup4>=4.12
markdownify>=0.13
PyYAML>=6
gradio>=4
