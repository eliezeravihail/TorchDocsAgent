# Single source of dependency truth is pyproject.toml [project.dependencies].
# HF Spaces installs from this file; the lone "." tells pip to install THIS
# project, which pulls those deps in — so there is no second list to drift out
# of sync (a missing google-genai here once silently broke the live Space).
.
