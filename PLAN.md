# Plan

## Initial state TODO

- [ ] Choose project structure and language/framework for the agent (e.g. Python package + CLI).
- [ ] Set up basic project scaffolding (dependency management, linting, test runner).
- [ ] Build a PyTorch docs ingestion pipeline (fetch/scrape docs pages, split into chunks).
- [ ] Store doc chunks in a vector index for retrieval (RAG).
- [ ] Integrate an LLM to answer questions using retrieved doc context.
- [ ] Add a simple CLI or chat loop for asking questions interactively.
- [ ] Add tests covering ingestion, retrieval, and answer generation.
- [ ] Document setup and usage in the README.
